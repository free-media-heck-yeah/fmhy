// docs/.vitepress/config.mjs
import { defineConfig } from 'vitepress'

export default defineConfig({
  // Nếu bạn cần base, theme, nav... đặt ở đây
  // Chỉ cần để mặc định, VitePress sẽ build docs/index.md
})
